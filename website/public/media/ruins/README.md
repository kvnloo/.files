# Forgotten Ruins web assets (local / private bake)

Generated by:

```sh
python3 website/scripts/unpack-wallpaper.py \
  --src /path/to/unpacked/workshop/2133182232 \
  --out website/public/media/ruins
```

**Do not commit** `base.webp`, masks, `waterripplenormal.webp`, or `scene.gen.json`
derived from Steam workshop art unless you own / re-author / have explicit license
to redistribute a downscaled bake.

The public site builds without these files: the WebGL player uses the shipped poster
as the base plate and procedural / white masks so water motion still demos.
