# Enable native Wayland + PipeWire screen capture for Google Chrome
# Without these, Chrome runs under XWayland and can't share screens/windows
# in Google Meet (only tabs are visible).

--ozone-platform-hint=auto
--enable-features=WebRTCPipeWireCapturer
