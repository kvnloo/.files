# Optional: Oh My Pi /live through Headphone DSP (AutoEQ + loudness + MBC).
#
# Renamed from 40-omp-live-clean.conf. That path is not dry — it still runs
# HD800S/Monarch AutoEQ, ISO 226 loudness, and MBC. Active OMP routing is
# 40-omp-live-dry.conf → raw DX5.
#
# To send live speech through DSP instead of DX5, disable 40-omp-live-dry.conf
# and uncomment the rules below, then restart pipewire-pulse.

# pulse.rules = [
#     {
#         matches = [ { application.name = "oh-my-pi" } ]
#         actions = {
#             update-props = {
#                 target.object = "effect_input.headphone_dsp"
#             }
#         }
#     }
# ]
