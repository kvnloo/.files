# =====================================================
# HYPRIDLE CONFIGURATION
# =====================================================
# Idle management for Hyprland
# See: https://wiki.hyprland.org/Hypr-Ecosystem/hypridle/
# =====================================================

general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}

# Dim screen after 5 minutes (300s)
listener {
    timeout = 300
    on-timeout = brightnessctl -s set 10
    on-resume = brightnessctl -r
}

# Lock screen after 10 minutes (600s)
listener {
    timeout = 600
    on-timeout = loginctl lock-session
}

# Turn off display after 12 minutes (720s)
listener {
    timeout = 720
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

# Suspend after 15 minutes (900s)
listener {
    timeout = 900
    on-timeout = systemctl suspend
}
