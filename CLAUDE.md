# Claude / agent notes for .files

Follow [AGENTS.md](AGENTS.md) for onboarding.

On first contact with this repository, run `./scripts/onboard status --json`. If onboarding is incomplete, ask the user whether they want to run setup now, then walk modules interactively (or direct them to `./install`).
